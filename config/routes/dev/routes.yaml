main:
    resource: ../../routes.yaml

controllers:
    resource: ../../../src/Controller/
    type: annotation

eldoPagesBundle:
    resource: "@eldoPagesBundle/Controller/"
    type: annotation
    prefix:   /

# JMSTranslationBundle Routing
JMSTranslationBundle_ui:
    resource: "@JMSTranslationBundle/Controller/"
    type:     annotation
    prefix:   /_trans
